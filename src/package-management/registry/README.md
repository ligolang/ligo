This library should ideally encapsulate all the registry API interactions and nothing more

As a consequence, `cohttp` dependency must only be present here and nowhere else. This is not so at the moment.
