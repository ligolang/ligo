type storage = int;

type parameter = list<int>;

const quarter = (n : int) : string => {
  let output = "";
  switch (n) {
    default:
      output = "Invalid month."
  };
  return output;
}

type return_ = [list<operation>, storage];

const main = ([a, b] : [parameter, storage]) : return_ =>
  [list([]) as list<operation>, 0]
