const main = ([p, s] : [bool, unit]):[list<operation>, unit] => {
  let _ : unit = assert (p);
  return [(list([]) as list<operation>), s];
};

const some = (o : option<unit>):unit => {
  assert_some(o)
};
