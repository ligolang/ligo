const f = "../../../../../src/contract/unit.mligo";

export const originate = (): address => {
  let [c_addr, _code, _size] =
    Test.originate_from_file(f, Test.eval(unit), 0mutez);
  return c_addr
};
