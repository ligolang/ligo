/* Test that the string concatenation syntax in JsLIGO works */

const length_op = (s: string) : nat => String.length (s);
const concat_op = (s: string) : string => s + "toto";
const sub_op = (s: string) : string => String.sub (1, 2, s);
