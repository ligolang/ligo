// see issue #1692

namespace Foo {
  let _ = 1;
  // rejected by the parser, but should work
  // let x = _
};

namespace Bar {
  const [_, ignored] = [1, 2]
  // rejected by the parser, but should work
  // const x = _
};

// rejected by the parser, but should work in principle
// type _ = int
// type y = _

// namespace _ {
// }
