/* Test use of multiple subroutines in a JsLIGO function */

const inc        = (i : int) : int => i + 1;
const double_inc = (i : int) : int => inc (i+1);
const foo        = (i : int) : int => inc (i) + double_inc (i);
