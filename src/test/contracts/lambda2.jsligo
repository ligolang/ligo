type storage = unit;

/* Not supported yet:
   let main (p:unit) storage = (fun x -> ()) ()
   */

let main = ([_, _] : [unit, storage]) : unit =>
  ((f : ((a: unit) => unit)) => f (unit)) ((_useless : unit) => unit);
