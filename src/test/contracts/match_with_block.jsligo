function match_with_block () {
  const x = 1;
  return $match(["Some" as "Some",1], {
           "None": () => failwith(1),
           "Some": (org) => (() => { let y = x + 1; return y })()
         });
}
