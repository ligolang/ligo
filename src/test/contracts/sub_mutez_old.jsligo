const sub = ([store, delta] : [tez, tez]) : tez => store - delta;

const main = ([_, store] : [unit, tez]) : [list<operation>, tez] => {
 return [ (list([]) as list <operation>), sub ([store, 1 as tez]) ]
};
