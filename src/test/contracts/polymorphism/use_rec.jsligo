import * as Contract from "rec.jsligo";

const _test = (_t : unit) : int => {
  let orig =
      Test.Originate.contract(contract_of(Contract), 0, 0 as tez);
  return orig.size;
};

const test = _test();
