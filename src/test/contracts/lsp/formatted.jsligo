#import "folding_range.jsligo" "M"

const increment = (b: int): int => ((a: int): int => a + 1)(b)

const with_semicolon = [1 + 2 * 3, M.fold_test];

const incr_map = (l: list<int>): list<int> => List.map((i: int) => i + 1, l)
