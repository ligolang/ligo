type t = { a: int, b: bool } | { a: int, b: string };

function f (x: int) {
  const record: t = { a: x, b: true };
  if (record.a == 1) return 3; else return 4;
}

class C {
  @entry
  entry = (_p: unit, s: int): [list<operation>, int] => [[], f(s)];
}
