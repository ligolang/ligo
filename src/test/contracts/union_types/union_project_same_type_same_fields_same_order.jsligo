function f = (x: int) {
  const record: { a: int, b: bool } | { a: int, b: string } = {
    a: x,
    b: true
  };
  return record.a;
}

class C {
  @entry
  entry = (_p: unit, s: int): [list<operation>, int] => [[], f(s)];
}
