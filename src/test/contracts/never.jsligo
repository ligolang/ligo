type parameter =
  ["Increment", int]
| ["Extend", never];

type storage = int;

class C {
  @entry
  main = (action: parameter, store: storage): [list<operation>, storage] =>
  [[],
   $match(action, {
     "Increment": n => store + n,
     "Extend": k => (Tezos.never(k) as storage)
   }
  ]
};
