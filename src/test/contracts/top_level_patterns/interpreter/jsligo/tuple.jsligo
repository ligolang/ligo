const _ = Test.set_print_values ()

const f = () => {
  Test.log("Once");
  return [1 as nat, 1, "Hello"]
}
const [a, b, c] = f ()

const [a1, b1, c1] = [1 as nat, 1, "Hello"]

const _test = () => {
    assert (a == a1);
    assert (b == b1);
    assert (c == c1)
}

const test = _test()