// @export
import * as A from "A.jsligo";

export const toto = 32
export const titi = A.toto + 42;

// @entry
export function main (_u: unit, x_: int) : [list<operation>, int] {
  const x : int = x_ + toto + titi;
  return [[], x];
};
