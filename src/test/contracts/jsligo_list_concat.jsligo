const xs = list([1, 2, 3]);
const ys = list([4, 5]);
const zs = list([...xs, ...ys]);

type ret = [list<operation>, list<int>]

class C {
  @entry
  main = (p: list<int>, s: list<int>): ret => [[], [...p, ...s]]
};
