type organization = {
   name : string,
   admins : int,
};
type storage = int;

@entry
const unique = (_p : organization, _s : storage) => {
    return failwith("You need to be part of Tezos organization to activate an organization");
};
