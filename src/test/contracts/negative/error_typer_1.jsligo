type action = unit;
type storage = nat;

const add_one = (oldStorage: nat) : nat => oldStorage + (1 as nat);

function main (param : action, oldStorage : storage) : [list<operation>, storage] {
  let newStorage : storage = add_one (oldStorage, 1 as nat);
  return [[], newStorage];
}
