/* eslint-disable import/prefer-default-export */

/**
 * The name of the extension's publisher. Will be displayed in Open VSX and
 * Visual Studio Code Marketplace.
 */
export const extensionPublisher = 'ligolang-publish'

/**
 * The name of the extension. Will be displayed in Open VSX and Visual Studio
 * Code Marketplace.
 */
export const extensionName = 'ligo-vscode'

/**
 * The ID of the extension. Will be displayed in Open VSX and Visual Studio Code
 * Marketplace.
 */
export const extensionId = `${extensionPublisher}.${extensionName}`

/**
 * The supported file extensions (.mligo and .jsligo).
 */
export const extensions = ['.mligo', '.jsligo'];
