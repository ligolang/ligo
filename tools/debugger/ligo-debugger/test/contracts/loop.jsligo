@entry
let main = (_ : unit, s : int) : [list<operation>, int] => {
  let acc : int = 0;
  let i = 1;

  while (i < s) {
    acc = acc + 1;
    i = i + 1;
  }

  return [(list([]) as list<operation>), acc]
}
