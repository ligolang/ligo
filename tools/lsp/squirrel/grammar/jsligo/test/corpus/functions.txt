
let full_name: full_name = ["foo", "bar", "baz"];


import US_DOLLAR = EURO;

let div = ([a, b]: [nat, nat]): option<nat> => {
  if(b == (0 as nat)){
    return (None() as option <nat>);
  } else {
    return (Some (a/b));
  };
};


let x : nat = 1 as nat;