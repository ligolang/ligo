=========
Includes
=========

#include "foo.bar"
#include "qux.ligo"

---

(source_file
  (include
    (String))
  (include
    (String))
)


=========
Function add
=========

function add (const a : int ; const b : int) : int is
  block { skip } with 1

---

(source_file
  (fun_decl
    (NameDecl)
    (param_decl (NameDecl) (TypeName))
    (param_decl (NameDecl) (TypeName))
    (TypeName)
    (let_expr
      (block (skip))
      (Int)
    )
  )
)
