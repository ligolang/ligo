const myFun = (x: int, y: int) => {
  const doubleX = x + x;
  const doubleY = y + y;
  return doubleX + doubleY;
};
function myFun2 (x: int, y: int) {
  const doubleX = x + x;
  const doubleY = y + y;
  return doubleX + doubleY;
}