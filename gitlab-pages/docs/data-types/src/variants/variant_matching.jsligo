type colour =
| ["RGB", [int, int, int]]
| ["Gray", int]
| ["Default"];

const int_of_colour = (c : colour) : int =>
  $match(c, {
    "RGB": ([r,g,b]) => 16 + b + g * 6 + r * 36,
    "Gray": i => 232 + i,
    "Default": () => 0
  });