const testDec = (() => {
  let v = 10;

  // Prefix decrement operator
  Assert.assert(--v == 9);
  Assert.assert(v == 9);

  // Postfix decrement operator
  Assert.assert(v-- == 9);
  Assert.assert(v == 8);
})();
