const testInc = (() => {
  let inc = 0;

  // Prefix increment operator
  Assert.assert(++inc == 1);
  Assert.assert(inc == 1);

  // Postfix increment operator
  Assert.assert(inc++ == 1);
  Assert.assert(inc == 2);
})();
